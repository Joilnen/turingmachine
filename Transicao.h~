#ifndef Transicao_H
#define Transicao_H

#include <map>
#include <string>

using namespace std;

class Transicao : public 
        map<pair<unsigned int, char>, pair<unsigned int, char> > {
    public:
        Transicao() { }
        ~Transicao() { }
};

#endif


