#ifndef CABECA_H
#define CABECA_H

#include <vector>

template <typename T> 
class Cabeca : public std::vector<T>::iterator {
    bool iniciada {false};
    public:
        Cabeca() : iniciada{c} { }
        ~Cabeca() { }
        bool foiInciada() { return iniciada; }
};

#endif


